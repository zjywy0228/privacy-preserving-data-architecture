"""Synthetic clinical dataset generator for testing and benchmarking.

Note: this package lives inside governance-templates/synthetic-data/ (both
directories use hyphens, which are not valid Python identifiers). Import by
adding the containing directory to sys.path:

    import sys
    sys.path.insert(0, "governance-templates/synthetic-data")
    from generate_synthetic_clinical import generate_synthetic_clinical
"""
